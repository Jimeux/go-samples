package point

type Point struct {
	X, Y int
}

// Add adds two Points together (implements the Summable interface)
func (p Point) Add(q Point) Point {
	return Point{
		X: p.X + q.X,
		Y: p.Y + q.Y,
	}
}
